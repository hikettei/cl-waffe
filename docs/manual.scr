@begin(section)
@title(Overview)

@begin(section)
@title(Welcome To cl-waffe)
  (Display my logo here)

  @b(This package is under development)

  cl-waffe is a deep learning framework, which is aimed to:
  
  @begin(enum)
  @item(Simple and fast.)
  @item(Integrate environment.)
  @item(Extensible for users.)
  @end(enum)

@end(section)
@end(section)